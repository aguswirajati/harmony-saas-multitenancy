# ============================================
# Harmony SaaS - Frontend Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values

# API Configuration
# Backend API base URL
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Application Configuration
NEXT_PUBLIC_APP_NAME=Harmony SaaS
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Sentry Error Tracking (optional - leave empty to disable)
# Get DSN from https://sentry.io
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_SENTRY_ENVIRONMENT=development
# SENTRY_ORG=your-sentry-org
# SENTRY_PROJECT=your-sentry-project

# Feature Flags (optional)
# NEXT_PUBLIC_ENABLE_ANALYTICS=false
# NEXT_PUBLIC_ENABLE_CHAT_SUPPORT=false

# ============================================
# Production Configuration Notes
# ============================================
# 1. Update NEXT_PUBLIC_API_URL to your production backend URL
# 2. Update NEXT_PUBLIC_APP_URL to your production frontend URL
# 3. Always use HTTPS in production (https://api.yourdomain.com)
# 4. Environment variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# 5. Never store secrets in NEXT_PUBLIC_ variables
# 6. Never commit .env.local to version control
